
1. **节点ID生成优化**：
   - 使用IP地址最后两个字节生成节点ID（0-1023）
   - 确保在局域网内节点ID唯一性

2. **mDNS增强**：
   - 在服务信息中添加节点名称、REST端口和启动时间戳
   - 支持从mDNS信息中提取节点名称和REST端口
   - 添加回退机制（mDNS失败时使用UDP多播）

3. **健康检查优化**：
   - 支持HTTPS和自定义健康检查路径
   - 通过环境变量配置协议和路径

4. **节点状态管理**：
   - 区分节点上线/下线事件
   - 节点下线时调用`ProposeLeave`
   - 添加详细的节点变更日志

5. **错误处理增强**：
   - 关键操作添加详细错误日志
   - mDNS初始化失败时回退到UDP多播
   - Kubernetes客户端初始化失败处理

6. **配置管理**：
   - 统一使用`getEnv`函数管理环境变量
   - 支持更多可配置项（REST端口、协议、健康检查路径等）

7. **优雅关闭**：
   - 添加更详细的关闭日志
   - 确保服务注销完成后再退出
   - 支持多种关闭信号（SIGINT, SIGTERM）

8. **代码结构优化**：
   - 减少锁持有时间
   - 优化节点清理逻辑
   - 改进事件通知机制

这些优化使服务发现模块更加健壮、可配置性更强，同时提高了在复杂网络环境下的可靠性。